<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- This allows sharing files from your app's private cache -->
    <cache-path name="cache" path="." />
    
    <!-- This allows sharing files from the public "Downloads" directory -->
    <external-path name="external_files" path="Download/"/>
    
    <!-- This allows sharing from the root of the external storage -->
    <external-files-path name="external_storage" path="."/>
</paths>